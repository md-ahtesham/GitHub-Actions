name: Install Node
on:
  push:
jobs:
  node-install:
    runs-on: ubuntu-latest
    steps:
      - name: install node
        uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: check version
        run: node --version